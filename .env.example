# Airtable
# When deploying on Railway, add these via the **Variables** tab or the CLI instead of committing a .env file.
# Railway automatically injects them into process.env for you at runtime.
AIRTABLE_API_KEY=pat_****************
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXX
AIRTABLE_TABLE_NAME=Restaurants

# Google Places (enable Places API + Photos)
GOOGLE_PLACES_API_KEY=AIza****************

# Worker behavior
DEFAULT_CITY=London
DEFAULT_COUNTRY=UK
MAX_RECORDS_PER_RUN=50
CONCURRENCY=2
SLEEP_MS_BETWEEN_REQUESTS=250

# Airtable select options (optional overrides)
# Provide a comma-separated list matching the "Enrichment Status" single-select values in Airtable
# AIRTABLE_ENRICHMENT_STATUS_OPTIONS=pending,enriched,not_found,error
# Provide a comma-separated list matching the "Instagram Status" single-select values
# AIRTABLE_INSTAGRAM_STATUS_OPTIONS=pending,found,not_found,error,retry

# Optional: generate short descriptions with OpenAI (comment out to disable)
# OPENAI_API_KEY=sk-****************

# Instagram enrichment script (optional)
# INSTAGRAM_CONCURRENCY=1
# INSTAGRAM_REQUEST_INTERVAL_MS=500

# Feed server (optional JSON API)
# START_MODE=server   # uncomment to make `npm start` launch the JSON feed instead of the worker
# PORT=3000
# RESTAURANTS_CACHE_TTL_MS=300000
